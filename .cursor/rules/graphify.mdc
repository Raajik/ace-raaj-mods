---
description: graphify knowledge graph context
alwaysApply: true
---

This project has a graphify knowledge graph at `A:/obsidian/jeremy/raw/graphify-out/` (moved out of the repo to keep the working tree clean).

- Before answering architecture or codebase questions, read `A:/obsidian/jeremy/raw/graphify-out/GRAPH_REPORT.md` for god nodes and community structure
- If `A:/obsidian/jeremy/raw/graphify-out/wiki/index.md` exists, navigate it instead of reading raw files
- After modifying code files in this session, run `graphify update .   --out-dir="A:/obsidian/jeremy/raw/graphify-out"` to keep the graph current (AST-only, no API cost)
- Alternatively, run from the repo root: `cd A:/ai/projects/ace-raaj-mods && graphify update . --out-dir="A:/obsidian/jeremy/raw/graphify-out"` or `uvx graphifyy update . --out-dir="A:/obsidian/jeremy/raw/graphify-out"`
