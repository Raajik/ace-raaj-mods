# Build outputs
[Bb]in/
[Oo]bj/
[Oo]ut/
[Ll]og/
[Ll]ogs/
[Dd]ebug/
[Rr]elease/
x64/
x86/
*.user
*.userosscache
*.suo
*.sln.docobj
.vs/
*.dll
*.pdb
*.cache
project.assets.json
*.nuget.g.props
*.nuget.g.targets
*.csproj.nuget.dgspec.json
*.AssemblyInfo.cs
*.GeneratedMSBuildEditorConfig.editorconfig
PublicizedAssemblies/
project.nuget.cache
.claude/

# IDE / OS / packages (consolidated from per-mod ignores)
.idea/
*.rsuser
packages/
*.nupkg
Thumbs.db
.DS_Store
desktop.ini
secrets.json

# Local env / sync config (never commit API keys)
.env.local
.env.*.local
linear-sync.config.json

# Symlinks / directory junctions (machine-local; Git has no "ignore symlinks" rule — list each path)
ACE-Server/

# Local ACE.Server drop for reference (not part of this repo)
ACE.Server-v1.76.4751/

# Local ACE source tree (optional clone; not part of this mod repo)
ACE-1.76.4751/

# Cursor agent plans (machine-local)
.cursor/plans/

# Scratch file lists from ripgrep / tooling
all_cs.txt
cs_files.txt

# WIP mods and experiments (not tracked until promoted out of this folder)
Work-In-Progress/

# Local reference material (ACE source, world DB dumps — not part of mod repo)
.references/
.worktrees/
graphify-out/

# Local mysqldump backups (Facility Hub and other SQL apply scripts)
WindblownContent/sql-backups/

# Bootstrap run logs (machine-local)
WindblownContent/scripts/*.log
