# AureatePath – TODO

No open backlog. Enlightenment luminance checks: `RequireAllLuminanceAuras`, `BaseLumAugmentationsRequired`, `RequireMinimumPerLumAugContributor`, `ShowLumAugVerificationCounts` in `Settings.json` / `Settings.cs`.
