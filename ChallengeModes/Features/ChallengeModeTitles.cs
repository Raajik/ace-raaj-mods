namespace ChallengeModes.Features;

// Future wishlist: apply a display title when activating /cm modes. ACE only supports CharacterTitle enum IDs
// for the nameplate (dat-driven strings); arbitrary text would need client/dat work or a different channel.
// Settings.ChallengeModeDefaultTitles remains in JSON for a future implementation (e.g. chat-only labels, or
// if ACE ever supports custom title strings). Do not call from commands until then.
public static class ChallengeModeTitles
{
}
