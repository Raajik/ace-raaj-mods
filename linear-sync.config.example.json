// Copy this file to linear-sync.config.json (repo root) and adjust values.
// Personal API key: Linear app -> Settings -> API -> Personal API keys. Export as LINEAR_API_KEY (never commit the key).
// Optional env overrides: LINEAR_SYNC_TEAM, LINEAR_SYNC_DEFAULT_PROJECT (useful in CI).
// Commands (from repo root):
//   dotnet run --project tools/LinearSync -- dry-run [--include-uncommitted]
//   dotnet run --project tools/LinearSync -- bootstrap
//   dotnet run --project tools/LinearSync -- push [--include-uncommitted]
{
  "team": "",
  "defaultProject": null,
  "baseRef": "origin/main",
  "repoUrl": null,
  "repoBranch": "main",
  "excludeModFolders": ["tools", "AceModQa"]
}
