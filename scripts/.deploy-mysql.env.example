# Copy this file to scripts/.deploy-mysql.env (gitignored) and fill in values.
# deploy-wb-test.sh, deploy-void-test.sh, and sync-world-void-test-to-wb-test.sh source it when present.
export ACE_MYSQL_USER=
export ACE_MYSQL_PASSWORD=
